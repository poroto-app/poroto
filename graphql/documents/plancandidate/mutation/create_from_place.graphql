mutation CreatePlanByPlace($sessionId: String!, $placeId: String!) {
    createPlanByPlace(
        input: {session: $sessionId, placeId: $placeId}
    ) {
        session
        plan {
            id
            name
            places {
                ...PlaceFullFragment
            }
            timeInMinutes
            transitions {
                duration
                from {
                    id
                }
                to {
                    id
                }
            }
        }
    }
}
