query CachedCreatedPlans($session: String!) {
    cachedCreatedPlans(input: {session: $session}) {
        plans {
            id
            name
            places {
                name
                photos
                location {
                    longitude
                    latitude
                }
            }
            timeInMinutes
        }
    }
}