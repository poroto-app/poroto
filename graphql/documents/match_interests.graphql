query MatchInterests($latitude: Float!, $longitude: Float!) {
    matchInterests(input: {latitude: $latitude, longitude: $longitude}) {
        session
        categories {
            name
            displayName
            photo
        }
    }
}