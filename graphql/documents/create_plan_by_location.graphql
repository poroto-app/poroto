mutation CreatePlanByLocation($latitude: Float!, $longitude: Float!, $categories: [String!], $planDuration: Int) {
    createPlanByLocation(input: {
        latitude: $latitude,
        longitude: $longitude,
        categories: $categories,
        freeTime: $planDuration,
    }) {
        session
        plans {
            id
            name
            places {
                name
                photos
                location {
                    longitude
                    latitude
                }
            }
            timeInMinutes
        }
    }
}