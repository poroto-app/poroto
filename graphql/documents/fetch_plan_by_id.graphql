query FetchPlanById($planId: String!) {
    plan(id: $planId) {
        id
        name
        places {
            id
            name
            photos
            location {
                longitude
                latitude
            }
            estimatedStayDuration
        }
        timeInMinutes
    }
}