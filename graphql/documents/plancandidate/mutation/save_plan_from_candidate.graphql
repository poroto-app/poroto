mutation SavePlanFromCandidate($session: String!, $planId: String!, $authToken: String) {
    savePlanFromCandidate(input: {session: $session, planId: $planId, authToken: $authToken}) {
        plan {
            id
        }
    }
}