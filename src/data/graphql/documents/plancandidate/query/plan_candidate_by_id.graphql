query PlanCandidate($input: PlanCandidateInput!) {
    planCandidate(input: $input) {
        planCandidate {
            plans {
                ...PlanFullFragment
            }
            createdBasedOnCurrentLocation
            likedPlaceIds
        }
    }
}