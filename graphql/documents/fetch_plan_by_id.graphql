query FetchPlanById($planId: String!) {
    plan(id: $planId) {
        id
        name
        places {
            name
            photos
            location {
                longitude
                latitude
            }
            estimatedStayDuration
        }
        timeInMinutes
    }
}