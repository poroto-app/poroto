query CachedCreatedPlans($session: String!) {
    cachedCreatedPlans(input: {session: $session}) {
        createdBasedOnCurrentLocation,
        plans {
            id
            name
            places {
                ...PlaceFullFragment
            }
            timeInMinutes
            transitions {
                duration
                from {
                    id
                }
                to {
                    id
                }
            }
        }
    }
}