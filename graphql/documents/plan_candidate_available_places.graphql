query FetchAvailablePlacesForPlanCandidate($session: String!) {
    availablePlacesForPlan(input: {session: $session}) {
        places {
            name
            id
            location {
                latitude
                longitude
            }
            photos
            thumbnails
            estimatedStayDuration
        }
    }
}